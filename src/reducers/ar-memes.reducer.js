const INITIAL_STATE = {
  title: 'Welcome...'
};

export const AR_MEMES = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    case '':
      break;

    default:
      break;
  }
  return state;
};
