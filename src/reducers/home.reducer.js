const INITIAL_STATE = {
  title: 'Welcome...'
};

export const HOME = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    case '':
      break;

    default:
      break;
  }
  return state;
};
